Wilkinson Workshop Test Plate v0.1.0

This directory stores the public download as base64 text chunks so the static
GitHub Pages site can assemble the exact versioned ZIP in the browser.

The product page verifies the ZIP byte count and SHA-256 digest before starting
the download when the browser supports Web Crypto.

Geometry validation passed. Physical resin print validation is pending.
